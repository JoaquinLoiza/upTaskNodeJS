doctype html
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario 
            h1 Iniciar Sesión en UpTask
            if locals.mensajes
                .alertas 
                    each categoria in Object.keys(locals.mensajes)
                        each mensaje in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= mensaje
            form#formulario.caja.caja-login(method='POST', action='/iniciar-sesion')
                .campo 
                    label(for='email') Email: 
                    input(type="text", name='email', placeholder='ejemplo@ejemplo.com' value= email)
                .campo 
                    label(for='password') Contraseña: 
                    input(type="password", name='password', placeholder='* * * * * * * * * * * * * *' value= password)
                .campo.enviar
                    input.boton(type="submit", value="Iniciar Sesión")
                .campo.acciones
                    a(href="/crear-cuenta") Crear una cuenta
                    a(href="/reestablecer") ¿Olvidaste tu contraseña?