extends layout

include mixins/crearProyecto

block contenido
    .contenido-principal
        h1= nombrePagina
        if errores 
            each error in errores 
                p.error.alerta= error.texto
        +crearProyecto(proyecto)